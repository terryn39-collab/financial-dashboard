# Financial Dashboard

Minimal Python foundation for investment-growth calculations.

## Run

```bash
python tracker.py --principal 10000 --rate 0.07 --years 10
```

The tracker calculates compound growth, creates annual simulation points, and appends each run to `growth_log.jsonl`.
